kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: switch
  lables:
    app: switch
spec:
  rules:
    - host: NAMESPACE.apps.sbc-okd.pcbltools.ru
      http:
        paths:
          - path: /api/v1
            pathType: Prefix
            backend:
              service:
                name: addresbook-main-service
                port:
                  name: addresbook-main-api


